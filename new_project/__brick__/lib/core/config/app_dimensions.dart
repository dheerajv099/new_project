import 'package:{{package_name}}/core/utils/app_size.dart';

abstract class AppDimens {
  /// Horizontal padding of the page
  static double horizontalPadding = 18.w;

  /// Horizontal padding of the page
  static double verticalPadding = 18.w;

  /// Radius of text shimmer
  static double mainTextShimmerRadius = 3;

  /// Radius of Product card shimmer
  static double productRatingShimmerRadius = 5;
}
